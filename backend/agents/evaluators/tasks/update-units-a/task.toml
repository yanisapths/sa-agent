schema_version = "1.4"

[task]
name = "sa-agent/update-units-a"
version = "1.0.0"
description = "Update Widget A units in an isolated sales.csv sandbox"
keywords = ["sandbox", "csv", "isolation"]

[metadata]
product = "Widget A"
units = 50

[verifier]
timeout_sec = 60.0

[agent]
timeout_sec = 180.0

[environment]
build_timeout_sec = 300.0
cpus = 1
memory_mb = 1024
