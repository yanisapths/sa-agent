Score whether the ACTUAL transcript satisfies the assertion.

The agent is a concise product chat assistant. It must reply with JSON
(`{"type":"text","text":"..."}`). Company facts must come from tool results,
not invention.

Respond with only 1 or 0, then a one-line comment.
