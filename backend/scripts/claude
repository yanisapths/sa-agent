#!/usr/bin/env bash
# Launch Claude Code already pointed at Bifrost.
set -euo pipefail
# shellcheck disable=SC1091
source "$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)/claude-bifrost.sh"
exec claude "$@"
